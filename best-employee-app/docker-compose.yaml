services:
  app:
    build: .
    restart: unless-stopped
    # Postgres runs natively on the EC2 host (see scripts/setup-ec2.sh), not
    # as a container. host.docker.internal lets the container reach it.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      POSTGRES_URL: postgres://jalastinvs:jalastinvs@localhost:5432/jalastinvs
      AUTH_SECRET: itsZmgdb6QXQuYyZolBNbiCAP6HO9N1rydPQiJsZWOY=
    ports:
      - "80:3000"
